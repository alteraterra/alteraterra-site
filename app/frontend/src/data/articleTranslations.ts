/**
 * Article translations — English only (no translations needed).
 * Kept as an empty export to avoid breaking imports.
 */

export type ArticleTranslations = Record<string, Record<string, never>>;

const articleTranslations: ArticleTranslations = {};

export default articleTranslations;