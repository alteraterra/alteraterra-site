import Layout from '@/components/Layout';
import Prelude from '@/components/Prelude';

export default function PreludePage() {
  return (
    <Layout>
      <h1 className="sr-only">Prelude</h1>
      <Prelude />
    </Layout>
  );
}